# Phase 2: server scaffold only. AI deps (boto3, ldai, launchdarkly-server-sdk)
# are added in Phase 3 when /chat is wired to AI Configs + Bedrock.

fastapi==0.136.1
uvicorn[standard]==0.47.0
python-dotenv==1.0.1
