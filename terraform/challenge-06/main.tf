# Challenge 06 — "How is Otto doing?"
#
# No new LaunchDarkly resources are created in this challenge. The setup work
# is done in setup-workstation, which runs the traffic generator to populate
# the monitoring view with metric events for the existing variations.
#
# This file exists so the per-challenge Terraform folder layout is consistent
# and `terraform init` works in setup-workstation even when there's nothing
# to apply.
